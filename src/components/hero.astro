---
import { Image } from "astro:assets";
// import HeroImage from "../assets/ramsey.jpg";
import HeroImage from "../assets/ramsey-portrait.jpg";
---

<section class={"hero"} id="hero">
  <div class={"hero__content"}>
    <h1 class={"titles"}>Hey, I’m Ramsey</h1>
    <p>
      I am a software engineer, building full-stack
      <span class="highlight"> websites and web apps</span>. I focus on clean UIs, solid
      architecture, and products that feel good to use.
    </p>
    <div class="hero__buttons">
      <a class={"btn"} href="#projects">View My Projects</a>
      <!-- <a class={"btn btn--secondary"} href="#contact">Get in touch</a> -->
    </div>
  </div>
  <div class={"hero__image"}>
    <Image src={HeroImage} alt="Ramsey Ediku" />
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 6fr 3.25fr;
    gap: var(--space-6);
    margin-block: var(--space-9) var(--space-12);

    .hero__content {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      margin-right: var(--space-4);
      /* heading sizing moved to BaseLayout utility classes (use class="titles") */

      p {
        font-weight: 300;
        margin: 0;
      }
    }

    .hero__image {
      display: flex;
      justify-content: flex-end;
      transform-origin: center center;
      img {
        --dimension: 17.5rem;
        width: var(--dimension);
        height: var(--dimension);
        border-radius: 50%;
        box-shadow: var(--box-shadow);
        &:focus,
        &:hover {
          cursor: pointer;
        }
        /* filter: brightness(1.01) contrast(1.05) saturate(1.03) sepia(0.01); */
      }
    }
  }

  .hero__buttons {
    display: flex;
    gap: var(--space-4);
  }
</style>

<script defer>
  let heroImage = document.querySelector(".hero__image > img");
  heroImage?.addEventListener("click", () => {
    heroImage.style.transform = "scale(1.1)";
  });
</script>
